Drop exported `.glb` or `.gltf` files in this folder for the project viewer.

Expected starter filenames from `src/components/sections/Projects.tsx`:
- `aquatonomous-hull.glb`
- `csa-robotic-arm.glb`
- `foil-gauge.glb`

Posters can stay in `public/images/` and are already wired in as fallbacks.
